#ifndef LOCATION_HPP
#define LOCATION_HPP

struct Location {
  int _idClient;
  int _idProduit;

  Noeud(int valeur, Noeud* suivant);
};

struct Liste {
  Noeud* _tete;

  Liste();
  ~Liste();
  void ajouterDevant(int valeur);
  int getTaille() const;
  int getElement(int indice) const;
};

#endif
